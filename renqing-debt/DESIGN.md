# 人情债管理系统 · 视觉规范

本软件的视觉唯一真相。**改样式之前先读这里**；要改规范本身，先改这份文件，再改代码，再改测试。

来源：Apple 官网设计系统分析（`DESIGN-apple.md`，原件存于站点仓库 `docs/design-systems/apple.md`）。这里记录的是**落到本软件的那一份**——只留用得上的，并写明我们在哪儿做了取舍。

> 本文件受自动化测试保护：站点仓库的 `tests/renqing-debt-tokens.spec.ts` 会对构建产物断言下面标 ✅ 的条目。改了规范而不改测试，构建会红。

---

## 1. 色

| 用途 | 值 | 说明 |
|---|---|---|
| **强调色 Action Blue** ✅ | `#0066cc` | **全系统唯一的强调色**。所有可点元素：主按钮、链接、关系人姓名、排序箭头 |
| 聚焦 Focus Blue | `#0071e3` | 只用于 `outline`（聚焦环）与选中态描边 |
| 深色面上的链接 | `#2997ff` | Action Blue 在近黑面上会沉下去，深色 tile 里的强调数字用它 |
| 墨色 Ink | `#1d1d1f` | 所有正文与标题（不是纯黑） |
| 次级墨色 | `#333333` | 次要文字、**送出**金额 |
| 弱墨色 | `#7a7a7a` | 标签、说明、禁用态 |
| 白 Canvas ✅ | `#ffffff` | hero、卡片、表格 |
| 羊皮纸 Parchment ✅ | `#f5f5f7` | 页面底色、点评块 |
| 珍珠白 Pearl | `#fafafc` | 次级按钮（存档区、翻页）填充 |
| 近黑 Tile ✅ | `#272729` | **人情资产负债表**（荒诞层）——全页唯一的深色面 |
| 近黑 Tile 2 | `#2a2a2c` | 深色面内的次级分区（免责声明条） |
| 发丝线 Hairline ✅ | `#e0e0e0` | 卡片、输入框、chips 的 1px 边 |
| 软分隔 Divider | `#f0f0f0` | 表格行分隔、次级按钮的环 |

**没有渐变。** 一个都没有。

### 收到 / 送出 怎么区分（本软件的取舍）

设计系统写死了「只有一个强调色，不存在第二个品牌色」，而账本必须区分方向。做法是：

- **方向的识别由「收到 / 送出」标签文字与金额正负号承担**，颜色只是辅助
- 收到 = Action Blue，送出 = 次级墨色 `#333333`
- **不新增第二个品牌色**

副作用是它比双色方案更可达：早先的绿/棕橙配色在红绿色盲下色差 ΔE 只有 4.0（安全线 8），换成现在这套后，识别根本不依赖颜色。

---

## 2. 字

字体栈：`-apple-system, BlinkMacSystemFont, "SF Pro Text/Display", system-ui, "PingFang SC", sans-serif`。macOS/iOS 上直接落到真正的 SF Pro；不装字体文件（自包含底线）。

| 用途 | 字号 / 字重 / 行高 / 字距 | 落在哪 |
|---|---|---|
| 页面标题 ✅ | 40 / 600 / 1.1 / **−0.4px** | 软件名 |
| 深色面标题 | 28 / 600 / 1.14 / −0.28px | 荒诞层判词、弹层标题 |
| 大数字 | 34 / 600 / 1.2 / −0.374px | 总账四格 |
| 章节标题 | 21 / 600 / 1.19 / 0.231px | 录入 / 总账 / 关系人 / 明细 / 存档 |
| 副文案 | 21 / 400 | hero 那句「一本正经地给人情往来记账」 |
| **正文** ✅ | **17** / 400 / 1.47 / −0.374px | 输入框、按钮、弹层正文 |
| 次级 | 14 / 400 / 1.43 / −0.224px | 表格、chips、筛选、翻页 |
| 强调次级 | 14 / 600 | 表头 |
| 微文案 | 12 / 400 / −0.12px | 标签、脚注、改动列、页脚 |

**硬规矩**

- **正文 17px，不是 16px** ✅——这是这套系统的签名节奏
- **显示级字号一律负字距** ✅（≥17px 用 −0.12 ~ −0.4px），12px 及以下不用
- **字重梯度只有 300 / 400 / 600 / 700，没有 500** ✅
- 标题用 600，不用 700
- **所有数字用 `font-variant-numeric: tabular-nums`** ✅——账目要对得齐

---

## 3. 形状

| 用途 | 圆角 |
|---|---|
| 主按钮、次级按钮、chips、输入框、标签 ✅ | 胶囊 `9999px` |
| 卡片（录入卡、表格卡、总账、荒诞 tile、弹层）✅ | `18px` |
| 次级方形按钮（存档、翻页） | `11px` |
| 二维码等内嵌图 | `8px` |

胶囊 = 可操作，18px 卡 = 容器。**别混用。**

---

## 4. 层次与阴影

**全系统零阴影** ✅。原系统只保留一个阴影，且只给产品照片用——本软件没有产品照片，所以一个都不该出现。

分层只靠三样：

1. **换面色**（白 ↔ 羊皮纸 ↔ 近黑）
2. **1px 发丝线**
3. **背景模糊**：弹层遮罩用 `backdrop-filter: blur(20px)` + 72% 羊皮纸

聚焦态用 `outline: 2px solid #0071e3` ✅，**不用 box-shadow**。

---

## 5. 间距

基数 8px。用到的档位：4 / 8 / 12 / 17 / 24 / 32 / 48。

- 卡片内边距 **24px**（深色 tile 32px）
- 章节间距 **32px**
- hero 上下 **64 / 48px**
- 表格单元格 `17px 20px`
- 按钮 `11px 22px`（次级 `8px 16px`）
- 内容最大宽 **980px**，hero 边到边（横贯整屏，靠色块换面当分隔，不画分隔线）

---

## 6. 组件语法

- **主按钮**：Action Blue 填充 + 白字 + 胶囊 + `11px 22px`；按下 `transform: scale(0.95)`
- **次级按钮**：透明底 + 蓝字 + 1px 蓝边 + 胶囊
- **工具按钮**（存档、翻页）：珍珠白底 + 次级墨字 + 11px 圆角
- **行内动作**（改 / 作废）：纯文字蓝链接，不做成按钮——表格行才不会被按钮撑高
- **chips**：白底 + 发丝线 + 胶囊；**选中态是 2px `#0071e3` 描边**（内边距减 1px 抵消，避免跳动）
- **带删除叉的自定义 chip**：`标签 + ×` 拼成一个胶囊，**边框整圈画在外层容器上，两个子元素一律不画边** ✅——各画半边会在 hover 时两半颜色对不上、接缝外露
- **输入框**：白底 + `rgba(0,0,0,0.08)` 边 + 胶囊 + 44px 高
- **表格**：白卡包裹，表头 14/600 弱墨色 + 底部发丝线，行间软分隔，最后一行无线

---

## 7. 移动端（≤834px）

- 录入字段 2 列，日期独占一行
- 总账 2×2
- 荒诞层单列
- hero 顶部留 88px 给固定的分享按钮
- **390px 下不允许出现横向滚动** ✅

---

## 8. 明确不做

- 装饰性渐变
- 卡片 / 按钮 / 文字的任何阴影
- 第二个品牌色
- 字重 500
- 图片圆角出现在非卡片语境
