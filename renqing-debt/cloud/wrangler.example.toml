# 云端版（自托管态的一种）的 Worker 配置模板。
# 用法：复制成 wrangler.toml，填自己的值。wrangler.toml 不进仓库（见 .gitignore），
# 因为里面是你自己的 Worker 名和资源 id。

name = "CHANGE-ME"                # 你的 Worker 名字
main = "src/index.js"
compatibility_date = "2026-09-03"

[assets]
directory = "./public"            # 把你要跑的静态页放这里
binding = "ASSETS"

# ⚠️ 这一行不能删：默认情况下静态资源会先于 Worker 命中，
# 也就是**绕过登录墙**。打开它，每个请求才都先过 authenticate()。
run_worker_first = true

# 账本存储。binding 必须叫 LEDGER——Worker 只认这个名字（src/index.js 读 env.LEDGER）。
# id 由 `npx wrangler kv namespace create LEDGER` 打印出来，把它填进来。
# 不配也能部署，只是账本存不了，接口会明说 501 而不是假装成功。
[[kv_namespaces]]
binding = "LEDGER"
id = "PASTE-THE-ID-HERE"

# 凭据不写在配置里，用 secret（存在 Cloudflare，不落任何文件）：
#   npx wrangler secret put AUTH_USER
#   npx wrangler secret put AUTH_PASSWORD
#
# 会话 cookie 的签名密钥。让它在你机器上生成、直接进 Cloudflare，
# 中间不落任何文件、也不出现在命令历史里：
#   openssl rand -base64 32 | npx wrangler secret put SESSION_SECRET
#
# 不配也能跑——那样只是退回「每次都输密码」，门照样关着。
# 换掉这个值会让所有已发出的会话立刻失效（这就是密钥轮换 / 一键踢下线）。
